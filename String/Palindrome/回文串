寻找回文串的问题核心思想是：从中间开始向两边扩散来判断回文串。

对于求最长的回文串来说:
    for 0 <= i < len(s):
        找到以 s[i] 为中心的回文串
        更新答案

回文串的长度可能是奇数也可能是偶数 abba这种情况来说 上面的算法就不满足

进行修改:
for 0 <= i < len(s):
    找到以 s[i] 为中心的回文串
    找到以 s[i] 和 s[i+1] 为中心的回文串
    更新答案